# Database Configuration
# Uses environment variables (for production) with local defaults (for development)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/flip_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:camp15sep2000}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
# Use 'create' or 'create-drop' to force table recreation with UUID columns
# Change back to 'update' after first successful run
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
# UUID Support - PostgreSQL will use UUID type for ID columns
spring.jpa.properties.hibernate.id.new_generator_mappings=true

# Server Configuration
server.port=${PORT:8080}

# JWT Configuration
jwt.secret.key=${JWT_SECRET:YA1r4ZUuAnTT9kXQJwQ6L9aAuHH0VL+ThaZ+6w49Jaw=}

# Barcode API Configuration (UPC Item DB)
# Free tier: 100 requests/day, no API key required
# Paid plans: Requires API key, see https://devs.upcitemdb.com/
barcode.api.url=https://api.upcitemdb.com/prod/trial
barcode.api.enabled=true
barcode.api.key=


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:flipapp.ng@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vzkfqgsajuwyabhl}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000